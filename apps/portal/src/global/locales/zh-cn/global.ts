const action = {}

const label = {}

const component = {
  input: {
    placeholder: '请输入'
  }
}

const message = {}

export default {
  action,
  label,
  component,
  message
} satisfies typeof import('../en-us/global').default
